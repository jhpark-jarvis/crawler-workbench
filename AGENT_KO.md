# crawler-workbench 작업 규칙

## 목표

이 저장소는 신뢰성 있는 웹 데이터 수집을 구현하고 검증하는 Python 작업대입니다.
각 시나리오는 동작하는 코드뿐 아니라, 수집 방식을 선택한 이유와 운영·유지보수
방법을 기록하는 것을 목표로 합니다.

목표는 최대한 많은 데이터를 모으는 것이 아닙니다. 상황에 맞는 수집 방식을
판단하고 그 선택의 장단점을 문서화하는 것이 목표입니다.

## 디렉토리 역할

- `apps/requests/`: 정적 HTML과 공개 API 수집 시나리오
- `apps/selenium/`: 브라우저 자동화와 실패 대응 시나리오
- `apps/playwright/`: 브라우저 자동화와 네트워크 응답 분석 시나리오
- `apps/pipeline/`: 검증, 저장, 스케줄링, 운영 시나리오

시나리오 코드는 해당하는 디렉토리 안에 둡니다. 공통 유틸리티는 실제로 두 개 이상
시나리오에서 필요해진 뒤에만 `apps/common/`에 추가합니다.

## 언어와 실행 환경

- 모든 시나리오는 Python으로 구현합니다. 브라우저 자동화도 Selenium과 Playwright의
  공식 Python 바인딩만 사용합니다.
- JavaScript/TypeScript는 이 저장소의 구현 언어가 아닙니다. 페이지 렌더링, API 요청,
  브라우저 동작을 이해해야 할 때만 읽고 분석합니다.
- 첫 시나리오에서 Python 실행 버전을 정하면 프로젝트 문서에 기록하고 이후 시나리오도 같은
  버전을 사용합니다.

## 수집 정책

1. 구현·검증에는 공개 API, 공개 접근이 의도된 페이지, 또는 로컬 fixture를 사용합니다.
   같은 기술을 보여줄 수 있다면 로컬 fixture를 우선합니다.
2. 수집 전 대상의 이용약관과 `robots.txt`를 확인하고, 결과를 각 시나리오의
   `README.md` 또는 `notes.md`에 기록합니다.
3. 로그인, CAPTCHA, 유료벽, 접근 제어, 안티봇 보호를 우회하지 않습니다. 사이트
   정책을 피하기 위한 프록시 또는 User-Agent 로테이션도 사용하지 않습니다.
4. 불필요한 개인정보, 비밀값, 쿠키, 인증 정보, 세션 토큰을 수집·저장·커밋·공유하지
   않습니다.
5. 명시적 timeout과 보수적인 요청 속도를 설정합니다. 일시적 실패만 제한된 횟수로
   backoff와 함께 재시도합니다.
6. `429`, 지속적인 `403`, 자동 수집 불허의 명확한 신호가 나타나면 수집을 중단하거나
   속도를 낮추고 그 판단을 기록합니다.

## 시나리오 기본 구성

완료된 시나리오에는 상황에 맞게 다음 항목을 포함합니다.

- `README.md`: 목적, 소스 유형, 실행 명령, 수집 정책 확인 결과
- `main.py` 또는 이름이 명확한 실행 진입점
- `requirements.txt` 또는 비표준 의존성 설치 방법
- `tests/`: 라이브 대상 없이 검증 가능한 파싱·검증·변환 로직의 테스트
- `sample_output/`: 작고 정제된 결과 예시 또는 fixture. 원본 개인정보는 저장하지 않음
- `notes.md`: 실패한 시도, 구조 변경 위험, 운영상 결정

파싱 로직을 라이브 웹사이트에서만 시험할 수 있게 만들지 않습니다. 가능하면 작고
정제된 HTML 또는 JSON fixture를 보관합니다.

## 구현 규칙

1. 수집, 파싱, 검증, 저장을 분리합니다. 브라우저 콜백이나 하나의 큰 함수에 전체
   파이프라인을 넣지 않습니다.
2. 동작하는 범위에서 가장 단순한 방법을 먼저 선택합니다.
   공개 API -> 직접 HTTP 요청 -> 브라우저 자동화 순서로 검토합니다.
3. 브라우저 자동화는 안정적인 locator와 명시적 대기 조건을 사용합니다. 고정 `sleep`은
   의도적으로 비교 실험을 하는 경우를 제외하고 사용하지 않습니다.
4. HTML, API payload, DB row는 신뢰할 수 없는 입력으로 취급합니다. 저장 전 필수 필드,
   타입, 범위, 중복, 결측을 검증합니다.
5. 반복 실행해도 안전하게 만듭니다. DB 또는 파일 저장 전 idempotency key 또는 중복
   처리 정책을 정의합니다.
6. 실패를 진단할 수 있는 맥락을 로그에 남깁니다. source, request/job ID, status, retry
   횟수, 소요 시간, 레코드 수, 실패 원인을 기록하되 비밀값과 개인정보 전체 payload는
   기록하지 않습니다.
7. 설정값은 환경 변수 또는 git에서 제외한 로컬 설정 파일에 둡니다. 설정이 생기면
   필요에 따라 `.env.example`만 커밋합니다.

## 시나리오 진행 순서

모든 시나리오는 다음 순서를 따릅니다.

1. 수집 계약을 정의합니다. 대상 필드, 소스, 갱신 주기, 성공 기준, 정책 제약을 적습니다.
2. 소스를 직접 살펴봅니다. DOM 구조, API/XHR 요청, 페이지네이션, 로딩 방식, 변경 가능
   지점을 확인합니다.
3. 수집 방식을 선택하고 짧은 이유를 기록합니다.
4. 가장 작은 end-to-end 경로를 구현합니다: 수집 -> 파싱 -> 검증 -> 저장.
5. timeout, 제한된 재시도/backoff, 빈 결과, 스키마 변경, 중복을 처리합니다.
6. fixture와 허용된 범위의 통제된 라이브 실행으로 검증합니다.
7. `notes.md`에 실패 원인, 변경 사항, 운영 시 모니터링 방법을 남깁니다.
8. 구현 회고를 합니다. 설계, 핵심 장단점, 남은 운영 위험을 기록합니다.

## 완료 기준

아래를 모두 만족해야 시나리오 완료입니다.

- 문서화된 명령으로 실행된다.
- 결과가 정의한 데이터 검증을 통과한다.
- 재실행해도 의도하지 않은 중복이 생기지 않는다.
- 예상 실패 경로를 테스트했거나, 테스트할 수 없는 이유와 대응을 설명할 수 있다.
- 비밀값, 쿠키, 개인정보, 큰 원본 산출물이 커밋되지 않았다.
- README와 notes만으로도 코드를 다시 열지 않고 설계와 운영 결정을 이해할 수 있다.

## 변경 관리

- 커밋은 하나의 시나리오 또는 하나의 공통 인프라 변경으로 범위를 제한합니다.
- 현재 시나리오를 통과시키기 위해 다른 시나리오를 수정하거나 삭제하지 않습니다.
- 완료를 말하기 전에 가장 작은 관련 검증을 실행합니다.
- 반복되는 결정이 프로젝트 전체 규칙이 되면 이 문서를 갱신합니다.
